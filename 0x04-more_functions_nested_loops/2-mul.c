#include "main.h"
#include <stdio.h>

/**
 * mul - check if c is digit
 * @a: first number
 * @b; second number
 * Return: result of multiplication of a and b
 */

int mul(int a, int b)
{
	return (a * b);
}
